# Matrix Discord Alternative Configuration
# Edit these values before running install script

# Main domain (your server)
DOMAIN="y7xyz.com"

# Subdomains
MATRIX_DOMAIN="matrix.${DOMAIN}"
ELEMENT_DOMAIN="element.${DOMAIN}"
LIVEKIT_DOMAIN="livekit.${DOMAIN}"
TURN_DOMAIN="turn.${DOMAIN}"

# Admin email (for SSL certs)
ADMIN_EMAIL="admin@${DOMAIN}"

# Server settings
ENABLE_REGISTRATION=true
MAX_UPLOAD_SIZE="100M"

# Branding
BRAND_NAME="Dragon Chat"
